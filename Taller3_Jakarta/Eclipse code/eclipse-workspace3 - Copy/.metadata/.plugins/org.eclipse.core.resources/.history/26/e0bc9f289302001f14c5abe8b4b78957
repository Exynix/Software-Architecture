package modelo;

import jakarta.ejb.Stateless;
import jakarta.persistence.EntityManager;
import jakarta.persistence.NoResultException;
import jakarta.persistence.PersistenceContext;
import jakarta.persistence.TypedQuery;

/**
 * Session Bean implementation class Datosusuario
 */
@Stateless
public class Datosusuario implements DatosusuarioRemote {

    /**
     * Default constructor. 
     */
    public Datosusuario() {
        // TODO Auto-generated constructor stub
    }

    @PersistenceContext
    private EntityManager entityManager;
    
	@Override
	public String obtenermensaje(int id) {
		 TypedQuery<String> query = entityManager.createQuery(
		            "SELECT u.nombre FROM usuario u WHERE u.id = :id", String.class);
		        query.setParameter("id", id);
		        
		        String nombre = null;
		        try {
		            nombre = query.getSingleResult();
		        } catch (NoResultException e) {
		            // Manejo de excepción si el usuario no es encontrado
		            // Aquí podrías lanzar una excepción específica o retornar un valor predeterminado
		        }
		        
		        return nombre;
	}
	

}
